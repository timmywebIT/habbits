'use client'

interface DashboardHabitProps {
    title: string
    icon: string
}

export function DashboardHabit({ title, icon }: DashboardHabitProps) {
    return (
        <div>
            
        </div>
    );
}